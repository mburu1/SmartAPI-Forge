/**
 * Base URL of the SmartAPIForge.Api instance this dashboard talks to.
 * Defaults to the HTTP profile pinned in launchSettings.json for local dev
 * (`dotnet run --project src/SmartAPIForge.Api`). Point this at your
 * deployed API's origin for other environments.
 */
export const API_BASE_URL = 'http://localhost:5080';
